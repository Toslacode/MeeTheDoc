# Source

Vendored from https://github.com/Leonxlnx/taste-skill (MIT), path
`skills/taste-skill/SKILL.md`, at commit "Add React Bits to sponsors"
(2026-08-18).

The upstream folder is named `taste-skill` while the skill's frontmatter
`name` is `design-taste-frontend`. Claude Code matches on folder name, so
the folder was renamed to match the declared name on install — the file
contents are otherwise unmodified.

Reviewed before install: instructions only, no scripts, no hooks, no MCP
declarations, no credential access, no prompt injection.
